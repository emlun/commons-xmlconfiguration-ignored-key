apply plugin: 'java'

repositories { mavenCentral() }

dependencies {
  testCompile 'junit:junit:4.11',
              'commons-configuration:commons-configuration:1.+',
              'commons-collections:commons-collections:3.+' // Transitive from commons configuration
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.4'
}
